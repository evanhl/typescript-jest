{
    "scripts": {
        "test": "jest --maxWorkers=2",
        "lint": "eslint --fix **/*.ts",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/jest": "^29.0.0",
        "jest": "^29.0.1",
        "ts-jest": "^28.0.8"
    },
    "dependencies": {
        "typescript": "^4.8.2"
    }
}
